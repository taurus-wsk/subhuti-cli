先不管ai 部分，怎么使用rust实现流水线自动执行脚步，
可以用于生产力，要考虑什么，可以使用什么设计模式，并且我希望也是可以闭环的架构
，对外暴露钩子，再接入ai来执行，配置文件我要用json格式写


策略模式（Strategy） —— 执行不同任务
观察者模式（Observer） —— 钩子系统
命令模式（Command） —— 封装执行动作
建造者模式（Builder） —— 构造流水线
上下文模式（Context） —— 数据传递、状态共享
责任链模式（Chain of Responsibility） —— 步骤流转

[配置层]    pipeline.json  →  解析
             ↓
[核心层]    Pipeline 流水线（编排、状态、闭环）
             ↓
[执行层]    Step 步骤 → Executor 执行器（策略模式）
             ↓
[扩展层]    Hook 钩子（观察者模式）
             ↓
[AI接入层]  预留（未来只插代码，不改架构）